node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
.vercel

# Cloned skill kits — third-party repos, not app source
ai-system-build/
anti-slop-main/
_archive/

# Verification harness: playwright-core and its screenshot output are
# regenerated on every run, not source.
verify/node_modules/
verify/out/
