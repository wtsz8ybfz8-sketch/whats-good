{
  "$comment": [
    "Project settings, committed on purpose. CLAUDE.md \u00a714.4 asked the user to throw",
    "these switches; they belong in the repo where every session gets them, not in one",
    "machine's settings.local.json.",
    "",
    "The SessionStart hook exists because the QA gate's install step is the single most",
    "common reason a session skips the gate and reports from a cheaper check instead.",
    "playwright-core lives in verify/, never in the root package.json (\u00a79).",
    "SessionEnd stops the dev server. Every session before this one left a ~250MB Vite tree running after the work was done; several accumulated in one sitting. Nothing about the task needed them alive."
  ],
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$CLAUDE_PROJECT_DIR\" && npm install --silent >/dev/null 2>&1; npm --prefix verify install --silent >/dev/null 2>&1; echo 'deps ready: root + verify (QA gate can run)'",
            "timeout": 180
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$CLAUDE_PROJECT_DIR\" && node verify/serve.mjs down >/dev/null 2>&1 || true",
            "timeout": 30
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(npm install*)",
      "Bash(npm --prefix verify install*)",
      "Bash(npm run*)",
      "Bash(npx esbuild*)",
      "Bash(npx vite*)",
      "Bash(npx tsc*)",
      "Bash(node verify/checks.mjs)",
      "Bash(node checks.mjs)",
      "Bash(node driver.mjs*)",
      "Bash(NO_PROXY=* *)",
      "Bash(grep*)",
      "Bash(git status*)",
      "Bash(git diff*)",
      "Bash(git log*)",
      "Bash(node verify/serve.mjs*)",
      "Bash(lsof*)",
      "Bash(ps *)",
      "Bash(pkill -f vite*)"
    ],
    "deny": [
      "Bash(rm *.git/index.lock)",
      "Bash(rm *.git/*.lock)"
    ]
  }
}