{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "rm -rf whats-good-next/node_modules && cd whats-good-next && bun install --frozen-lockfile",
  "buildCommand": "cd whats-good-next && NITRO_PRESET=vercel bun run build && cd .. && rm -rf .vercel/output && mkdir -p .vercel && cp -r whats-good-next/.vercel/output .vercel/output"
}
