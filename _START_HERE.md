# Start here

**Read [CLAUDE.md](CLAUDE.md). It is the single authoritative instruction file** — product
positioning, operating mode, validation ladder, design system and hard rules all live there.

- **[CLAUDE.md](CLAUDE.md)** — how to work on this project. Start and stay here.
- **[HANDOVER.md](HANDOVER.md)** — live state of the last session only. A record, not a mandate.
- **[README.md](README.md)** — how to run it locally.

This folder is ONE project: the **What's Good** app (mood-and-location food discovery). It is
unrelated to the `work-os` / "Claude Code" folder, which is a separate app.

`_archive/` holds files tidied on 2026-07-09 (a junk-named `'.md`, a duplicate
`package-lock 2.json`, one-off scripts `fix.js` / `replace-colors.js`, an old
`restaurant-first-preview.html`). Nothing was deleted — delete `_archive/` yourself once
you're sure.
